import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'les1-voorbeeld1-ionic',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
